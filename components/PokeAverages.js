const PokeAverages = (height, weight, limit) => {
    // console.log(`Height: ${height}`)
    // console.log(`Weight: ${weight}`)
    // console.log(`Limit: ${limit}`)
    // let heightSum = height.reduce((partialSum, a) => partialSum + a, 0);
    // let weightSum = weight.reduce((partialSum, a) => partialSum + a, 0);
    // console.log(heightSum);
    // console.log(weightSum);
    // console.log("Height Average: ", (heightSum / limit));
    // console.log("Weight Average: ", (weightSum / limit));
}

export default PokeAverages;